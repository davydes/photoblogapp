<div class="container-fluid">
  <div class="page-header">
    <h3><%=t 'photos.label.photos'%> - "<%= @owner.name %>"</h3>
  </div>

  <%= render partial: 'photos/justified_gallery', :locals => {:photos => @photos} %>
</div>