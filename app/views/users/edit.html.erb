<%- model_class = User -%>
<%= provide(:title, t('users.edit_user', :name => @user.name) )%>
<div class="container">
<div class="page-header">
  <h1><%=t 'users.edit_user', :name => @user.name %></h1>
</div>
<%= render partial: 'form', locals: {mode: 'edit'} %>
</div>