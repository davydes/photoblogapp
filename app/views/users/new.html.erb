<%= provide(:title, t('sessions.signup')) %>

<div class="wrap-user">
  <div class="page-header">
    <h1><%=t('sessions.signup')%></h1>
  </div>
</div>
<%= render partial: 'form', locals: {mode: 'create'} %>