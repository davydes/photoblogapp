<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <div id="logo-icon"></div>
      <%= link_to "Light Painting", root_path, class: 'navbar-brand' %>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-main">
      <ul class="nav navbar-nav navbar-right ">
          <li><%= link_to t('articles.label.articles'), explorer_articles_path %></li>
          <li><%= link_to t('articles.label.sandbox'), explorer_articles_in_sandbox_path %></li>
          <li><%= link_to t('photos.label.photos'), explorer_photos_path %></li>
          <li><%= link_to t('users.label.users'), users_path %></li>
          <% if signed_in? %>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= image_tag image_avatar_url(current_user), class: "img-circle", id: "panel-avatar" %>
                  <%= current_user.name %><b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to t('articles.label.new'), new_article_path %></li>
                  <li><%= link_to t('photos.label.upload'), new_photo_path %></li>
                  <li class="divider"></li>
                  <li><%= link_to photos_path do %>
                    <%= badge(current_user.photos.count) %>
                    <%= t('photos.label.photos') %>
                  <% end %></li>
                  <li><%= link_to albums_path do %>
                    <%= badge(current_user.albums.count) %>
                    <%= t('albums.label.albums') %>
                  <% end %></li>
                  <li><%= link_to articles_path do %>
                    <%= badge(current_user.articles.count) %>
                    <%= t('articles.label.articles') %>
                  <% end %></li>
                  <li class="divider"></li>
                  <li><%= link_to t('sessions.profile'), current_user %></li>
                  <li><%= link_to t('sessions.settings'), edit_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to t('sessions.logout'), signout_path %>
                  </li>
                </ul>
              </li>
          <% else %>
              <li><%= link_to t('sessions.login'), signin_path %></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>