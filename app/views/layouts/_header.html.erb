<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
      <%= link_to "BLOG APP", root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to t('root_page'), root_path %></li>
          <% if signed_in? %>
              <li><%= link_to t('users.label'), users_path %></li>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%=t('sessions.account')%> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to t('sessions.profile'), current_user %></li>
                  <li><%= link_to t('sessions.settings'), edit_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to t('sessions.logout'), signout_path %>
                  </li>
                </ul>
              </li>
          <% else %>
              <li><%= link_to t('sessions.login'), signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</nav>